-- CREATE DATABASE ACTIVIDAD_1_1
-- GO
-- USE ACTIVIDAD_1_1
-- CREATE TABLE CURSOS(
--     ID INT NOT NULL PRIMARY KEY,
--     Nombre VARCHAR(100) NOT NULL,
--     CostoCurso MONEY NOT NULL,
--     CostoCertificado MONEY NOT NULL
-- )
-- CREATE TABLE CATEGORIAS(
--     ID INT NOT NULL PRIMARY KEY,
--     Nombre VARCHAR(100) NOT NULL,
-- )
-- CREATE TABLE IDIOMAS(
--     ID INT NOT NULL PRIMARY KEY,
--     Nombre VARCHAR(100) NOT NULL,
-- )
-- CREATE TABLE NIVELES(
--     ID INT NOT NULL PRIMARY KEY,
--     Nombre VARCHAR(100) NOT NULL
-- )
-- CREATE TABLE TIPOS_DE_CONTENIDOS(
--     ID INT NOT NULL PRIMARY KEY,
--     Nombre VARCHAR(100) NOT NULL
-- )
-- CREATE TABLE CONTENIDOS(
--     ID INT NOT NULL PRIMARY KEY,
--     Tipo INT NOT NULL FOREIGN KEY REFERENCES TIPOS_DE_CONTENIDOS(ID),
--     Tamanio INT NOT NULL
-- )
-- CREATE TABLE CLASES(
--     ID INT NOT NULL PRIMARY KEY,
--     Nombre VARCHAR(100) NOT NULL,
--     NumClase INT NOT NULL,
--     Duracion INT NOT NULL
-- )
-- CREATE TABLE CLASE_PERTENECE_A_CURSO(
--     IdClase INT NOT NULL FOREIGN KEY REFERENCES CLASES(ID),
--     IdCurso INT NOT NULL FOREIGN KEY REFERENCES CURSOS(ID),
--     PRIMARY KEY(IdClase)
-- )
-- CREATE TABLE NIVELES_DE_CURSO(
--     IdCurso INT NOT NULL PRIMARY KEY FOREIGN KEY REFERENCES CURSOS(ID),
--     IdNivel INT NOT NULL FOREIGN KEY REFERENCES NIVELES(ID)
-- )
-- CREATE TABLE CURSO_CON_CATEGORIA(
--     IdCurso INT NOT NULL FOREIGN KEY REFERENCES CURSOS(ID),
--     IdCategoria INT NOT NULL FOREIGN KEY REFERENCES CATEGORIAS(ID),
--     PRIMARY KEY(IdCurso, IdCategoria)
-- )
-- CREATE TABLE AUDIO_CURSOS(
--     IdCurso INT NOT NULL FOREIGN KEY REFERENCES CURSOS(ID),
--     IdIdioma INT NOT NULL FOREIGN KEY REFERENCES IDIOMAS(ID),
--     PRIMARY KEY(IdCurso, IdIdioma)
-- )
-- CREATE TABLE SUBTITULOS_CURSOS(
--     IdCurso INT NOT NULL FOREIGN KEY REFERENCES CURSOS(ID),
--     IdIdioma INT NOT NULL FOREIGN KEY REFERENCES IDIOMAS(ID),
--     PRIMARY KEY(IdCurso, IdIdioma)
-- )
-- CREATE TABLE CONTENIDOS_EN_CLASES(
--     IdContenido INT NOT NULL FOREIGN KEY REFERENCES CONTENIDOS(ID) PRIMARY KEY,
--     IdClase INT NOT NULL FOREIGN KEY REFERENCES CLASES(ID)
-- )